# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vite/
docs/.docs-queue

# Claude Code agent worktrees — never commit; a stray gitlink here breaks every
# clone that initialises submodules (it has no .gitmodules entry). This broke the
# first Cloudflare Pages build: "fatal: No url found for submodule path".
.claude/worktrees/
